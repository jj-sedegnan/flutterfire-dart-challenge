name: flutterfire_dart_challenge
description: A clean Dart OOP project implementing generic repository pattern, custom exceptions, and unit tests for NextFlutter FlutterFire Summer Camp 2026.
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
